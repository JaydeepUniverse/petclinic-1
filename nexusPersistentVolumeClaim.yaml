apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nexus
  namespace: nexus
spec:
  storageClassName: nexus
  resources:
    requests:
      storage: 50Gi
  accessModes:
    - ReadWriteOnce
